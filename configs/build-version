# Identity and release data for the generated version header. Reviewed data,
# not code: leonos-version reads it so that no product name or version number is
# duplicated inside a C constant (plan section 7).
kernel_name=ntclks
release_version=4.7.2
