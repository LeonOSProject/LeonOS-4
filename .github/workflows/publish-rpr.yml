name: Publish LeonOS RPR

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: leonos-rpr-pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    environment:
      name: github-pages
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: recursive
          persist-credentials: false

      - name: Install build dependencies
        run: |
          set -euo pipefail
          sudo apt-get update
          sudo apt-get install -y \
            build-essential clang libclang-rt-dev llvm lld \
            grub-efi-amd64-bin grub-pc-bin grub-common xorriso mtools \
            dosfstools e2fsprogs libext2fs-dev fdisk fakeroot gdisk qemu-utils zip \
            git make bison flex bc pkg-config autoconf automake libtool \
            libtool-bin gawk gperf gettext libncurses-dev ncurses-bin exfatprogs openssl perl
          msgfmt --version

      - name: Import APK signing key
        id: signing_key
        env:
          APK_SIGNING_KEY_B64: ${{ secrets.LEONOS_APK_SIGNING_KEY_B64 }}
        run: |
          set -euo pipefail
          test -n "$APK_SIGNING_KEY_B64" || {
            echo 'Missing GitHub Secret: LEONOS_APK_SIGNING_KEY_B64' >&2
            exit 1
          }
          key_path="$RUNNER_TEMP/leonos-rpr-signing-key.rsa"
          umask 077
          printf '%s' "$APK_SIGNING_KEY_B64" | base64 --decode > "$key_path"
          chmod 0600 "$key_path"
          openssl pkey -in "$key_path" -noout -check >/dev/null
          echo "path=$key_path" >> "$GITHUB_OUTPUT"

      - name: Build signed RPR site
        env:
          LEONOS_APK_SIGNING_KEY: ${{ steps.signing_key.outputs.path }}
        run: |
          set -euo pipefail
          make fetch
          make defconfig
          make -j"$(nproc)" APK_SIGNING_KEY="$LEONOS_APK_SIGNING_KEY" rpr-pages

      - name: Verify publication boundary
        run: |
          set -euo pipefail
          test -s out/x86_64/release/rpr-pages/apk/packages.adb
          test -s out/x86_64/release/rpr-pages/apk/leonos-rpr.rsa.pub
          test -s out/x86_64/release/rpr-pages/kernel/kernel.sys
          test -s out/x86_64/release/rpr-pages/kernel/loader.elf
          test -s out/x86_64/release/rpr-pages/kernel/release.txt
          if grep -R -E -- 'BEGIN (RSA |EC |OPENSSH )?PRIVATE KEY' out/x86_64/release/rpr-pages; then
            echo 'Private key material reached the Pages directory' >&2
            exit 1
          fi

      - name: Remove private key
        if: always() && steps.signing_key.outputs.path != ''
        env:
          KEY_PATH: ${{ steps.signing_key.outputs.path }}
        run: rm -f -- "$KEY_PATH"

      - name: Configure GitHub Pages
        uses: actions/configure-pages@v5

      - name: Upload GitHub Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: out/x86_64/release/rpr-pages

      - name: Deploy GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
